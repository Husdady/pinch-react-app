// Constants
import { TOKEN, CALLER_ID, APPLICATION_ID } from "../../assets/data/api";

export const SEND_DATA_TO_WIX = {
  applicationId: APPLICATION_ID,
};

export const DEFAULT_WIX_RESPONSE = {
  token: TOKEN,
  callerId: CALLER_ID,
  userName: "User",
  userAvatar: null,
};
